# GigaChat 429 protection

HTTP 429 теперь не запускает следующий chunk. Тот же запрос повторяется с backoff, затем обработка останавливается при исчерпании повторов. Между чанками есть небольшая пауза.
